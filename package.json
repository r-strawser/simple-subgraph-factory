{
  "name": "simple-subgraph-factory",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ r-strawser/simple-subgraph-factory",
    "create-local": "graph create --node http://localhost:8020/ r-strawser/simple-subgraph-factory",
    "remove-local": "graph remove --node http://localhost:8020/ r-strawser/simple-subgraph-factory",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 r-strawser/simple-subgraph-factory",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.1",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
